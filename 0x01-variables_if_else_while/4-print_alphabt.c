#include <stdio.h>

/**
*main - entry point
*
*description: print the alphabet by putchar
*
*Return: 0 (seccess)
*/

int main(void)
{
	char ch = 'a';

	while (ch <= 'z')
{
	if
	(ch == 'e' || ch == 'q');
	ch++;
	putchar(ch);
	ch++;
}
putchar('\n');
return (0);
}
